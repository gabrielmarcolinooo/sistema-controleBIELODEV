services:
  - type: web
    name: sistema-controle-backend
    runtime: node
    rootDir: backend
    buildCommand: npm install && npm run build
    startCommand: npx prisma migrate deploy && node dist/server.js
    healthCheckPath: /
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: PORT
        value: 10000